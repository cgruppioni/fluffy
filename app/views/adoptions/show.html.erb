<div>
  <h3><%= @animal.name %></h3>
  <h4>Level: <%= @adoption.level %></h4>
  <h4>Points: <%= @adoption.score %></h4>
  <%= image_tag @animal.image.thumb.url.to_s %><br>
  
  <% if @adoption.hungry? %>
    <%= form_tag [@adoption, :feedings] do %>
      <%= submit_tag "Feed #{@animal.name}" %>
    <% end %>
  <% else %>
     Don't overfeed!
  <% end %>

  <hr>
  <%= button_to "Abandon Pet", @adoption, method: :delete %>
</div>
